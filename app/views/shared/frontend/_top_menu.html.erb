<div class="tabs is-centered">
  <ul> 
    <% categories.each do |category|  %>
      <li><%= link_to category.name, category %></li>
      <!-- category_path(category) 可簡寫為 category -->
    <% end %>
  </ul>
</div>