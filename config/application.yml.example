# Add configuration values here, as shown below.
# 此檔案非 原本figaro產生的apllication.yml檔，所以會被git追蹤
# 使用時 請把副檔名.sample拿掉，並在以下設定對應的值

# 註冊登入Google Develop Console可申請
google_app_id: "Your app id"
google_app_secret: "Your app secret"
# 設定之後 在controller或整個Rails網站裡面（全域環境變數），就可以透過 ENV['google_app_id']拿到 google_app_id 在這裡對應設定的值
# 這裡是設定檔，更動後要重啟伺服器才會生效
